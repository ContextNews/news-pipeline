storage: s3

# Clustering window (days)
window: 1

# HDBSCAN parameters
hdbscan:
  min_cluster_size: 3
  min_samples: 2

# Location aggregation parameters
location:
  min_confidence: 0.65
  max_locations: 10
  max_regions: 5
  max_cities: 5

output_format: parquet
