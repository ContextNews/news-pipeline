storage: local
input_dir: tests/data
output_dir: tests/output
spacy_model: sm
output_format: json

# NER is enabled by default via spacy_model above

# Embeddings disabled for service tests
embeddings:
  enabled: false
